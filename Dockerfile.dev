FROM node:alpine

RUN apt-get update -qq && apt-get install -y build-essential libpq-dev nodejs && apt-get install npm

WORKDIR /app/frontend

COPY ./app/Gemfile /webapp/Gemfile
COPY ./app/Gemfile.lock /webapp/Gemfile.lock

COPY ./package.json ./

RUN npm install

COPY ./ ./


CMD npm run start
